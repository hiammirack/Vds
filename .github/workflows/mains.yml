name: RDP

on:
  workflow_dispatch:

jobs:
  run-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: RDP Ayarlari
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Kullanicilari Olustur
        run: |
          Add-Type -AssemblyName System.Security
          $u = @()
          for ($i = 1; $i -le 4; $i++) {
              $n = "FlbsVds$i"
              $p = -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 12 | ForEach-Object {[char]$_})
              $s = ConvertTo-SecureString $p -AsPlainText -Force
              if (Get-LocalUser -Name $n -ErrorAction SilentlyContinue) { Remove-LocalUser -Name $n }
              New-LocalUser -Name $n -Password $s
              Add-LocalGroupMember -Group "Administrators" -Member $n
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member $n
              $u += "Kullanici: $n | Sifre: $p"
          }
          $d = "CRED_BLOCK"
          echo "RDP_C<<$d" >> $env:GITHUB_ENV
          $u -join "`n" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          echo "$d" >> $env:GITHUB_ENV

      - name: Tunel Baslat
        run: |
          Start-Process -FilePath "ssh.exe" -ArgumentList "-o StrictHostKeyChecking=no -p 80 tcp://a.pinggy.io:0:localhost:3389" -NoNewWindow
          Start-Sleep -Seconds 10

      - name: Bilgileri Goster
        run: |
          Write-Host "=== RDP BILGILERI ==="
          Write-Host "$env:RDP_C"
          while ($true) { Start-Sleep -Seconds 300 }
          
