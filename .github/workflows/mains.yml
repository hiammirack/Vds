name: RDP

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          $rule = Get-NetFirewallRule -Name "RDP-External" -ErrorAction SilentlyContinue
          if ($rule) {
              netsh advfirewall firewall delete rule name="RDP-External"
          }
          netsh advfirewall firewall add rule name="RDP-External" `
            dir=in action=allow protocol=TCP localport=3389

          Restart-Service -Name TermService -Force

      - name: Create Multiple FlbsVds Users
        run: |
          Add-Type -AssemblyName System.Security
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64) + [char[]](91..96) + [char[]](123..126))
          }

          $allCreds = @()

          for ($i = 1; $i -le 4; $i++) {
              $username = "FlbsVds$i"
              
              $rawPassword = @()
              $rawPassword += $charSet.Upper | Get-Random -Count 4
              $rawPassword += $charSet.Lower | Get-Random -Count 4
              $rawPassword += $charSet.Number | Get-Random -Count 4
              $rawPassword += $charSet.Special | Get-Random -Count 4
              $password = -join ($rawPassword | Sort-Object { Get-Random })
              $securePass = ConvertTo-SecureString $password -AsPlainText -Force
              
              if (Get-LocalUser -Name $username -ErrorAction SilentlyContinue) {
                  Remove-LocalUser -Name $username
              }

              New-LocalUser -Name $username -Password $securePass -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member $username
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username

              $allCreds += "Kullanici: $username | Sifre: $password"
          }

          $delimiter = "EOF_CREDENTIALS"
          echo "RDP_CREDS<<$delimiter" >> $env:GITHUB_ENV
          $allCreds -join "`n" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          echo "$delimiter" >> $env:GITHUB_ENV

      - name: Download and Run Playit.gg (TCP Tunnel)
        run: |
          $playitDir = "C:\Playit"
          New-Item -ItemType Directory -Force -Path $playitDir | Out-Null
          
          # Playit.gg Windows uygulamasını indiriyoruz
          $playitUrl = "https://github.com/playit-cloud/playit-agent/releases/download/v0.15.2/playit-0.15.2-windows-x86_64.exe"
          Invoke-WebRequest -Uri $playitUrl -OutFile "$playitDir\playit.exe"

          # Arka planda çalıştır
          Start-Process -FilePath "$playitDir\playit.exe" -NoNewWindow
          
          Start-Sleep -Seconds 10

      - name: Get Connection Address
        run: |
          Write-Host "`n=== HERKESE ACIK RDP EKRANI (PLAYIT) ==="
          Write-Host "Playit.gg baslatildi. GitHub loglarinda cikan 'Allocated port' veya 'Address:' kısmındaki TCP adresini (ornek: xxxx.ply.gg:12345) kopyalayin."
          Write-Host "---------------------------------"
          Write-Host "$env:RDP_CREDS"
          Write-Host "=================================`n"
          
          while ($true) {
              Write-Host "[$(Get-Date)] VDS Havuzu Aktif ve Disaridan Erisilebilir"
              Start-Sleep -Seconds 300
          }
